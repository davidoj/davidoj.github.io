@font-face {
  font-family: DejaVu-larrow;
  src: url(Metrickal-larrow.ttf);
}
html,
body {
  height: 100%;
}
body {
  color: #aaa;
  background-color: #000;
  font-size: 19px;
  font-family: Metrickal, droid sans mono, monospace;
  line-height: 1.3;
  margin: 0;
  box-sizing: border-box;
}